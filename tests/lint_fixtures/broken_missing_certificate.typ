// Pairs with clean_single.json (overall_score 82). Deliberately renders a
// different certificate word than what that score implies, and this comment
// itself must avoid spelling out the implied word so the fixture doesn't
// accidentally satisfy the substring check via its own prose.
#cover-page(
  score: 82,
  certificate: "STABIL",
)
